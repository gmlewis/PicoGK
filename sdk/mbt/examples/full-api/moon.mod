// full-api: PicoGK FFI example for MoonBit

name = "gmlewis/picogk-full-api-example"

version = "0.1.0"

license = "Apache-2.0"

description = "full-api example for MoonBit using FFI"

preferred_target = "native"

supported_targets = "+native"

import {
  "gmlewis/picogkffi@0.1.0",
}
